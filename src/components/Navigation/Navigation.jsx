import styles from "./navigation.module.css"

const Inventory = () => {
    return ( 
        <nav className={styles.nav}>
            <h1>Inventory</h1>
        </nav>
    );
}
 
export default Inventory;